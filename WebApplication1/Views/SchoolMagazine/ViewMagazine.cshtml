@*<table class="table table-bordered table-dark" style="width:max-content">
   
    <tr>
        <th>ФИО / Дата</th>
        @foreach (var col in ViewBag.dates)
        {
            <th width="10"><input class="table-dark" width="10" size="3" type="text" readonly value="@col.Date" /></th>
        }
    </tr>
    @foreach (var item in ViewBag.m)
    {
            <tr>
                <td width="300" style="vertical-align: top; font-size: 12px; text-transform: uppercase; color: white; margin: 0 15px">
                    @item.PupilId
                </td>
                <td width="280" style="vertical-align: top; font-size: 12px; text-transform: uppercase; color: white; margin: 0 15px">
                    @item.Mark
                </td>
            </tr>
}
    </table>*@
<table class="table table-bordered table-dark" style="width:max-content">


    @foreach (var item in ViewBag.comps)
    {
        <tr>@item.PupilId</tr>
    }
</table>